//!
//! Picture and scratch storage are now represented by owned Rust containers
//! (`Vec<u8>`, `Vec<u16>`, stack arrays, or typed frame structures), so this
//! module is intentionally not compiled from `lib.rs`.
